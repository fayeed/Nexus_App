<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/voilet</item>
        <item name="android:colorForeground">@color/voilet</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorAccent">@color/common_google_signin_btn_text_dark_focused</item>
        <item name="colorPrimaryDark">@color/com_facebook_button_background_color_disabled</item>
        <item name="colorPrimary">@color/cardview_light_background</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="nexus" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="colorAccent">@color/common_google_signin_btn_text_light_pressed</item>
    </style>

</resources>
